<div class="container">

  <table>
    <tr>
      <th>First Name</th>
      
      <th>Email</th>
    </tr>

    <% @contacts.each do |contact| %>
      <tr>
        <td><%= contact.firstname %></td>
      
        <td><%= contact.email %></td>
      </tr>
    <% end %>
  </table>

</div>
